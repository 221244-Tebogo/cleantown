# --- Dependencies / build outputs ---
node_modules/
dist/
web-build/
coverage/
.npm/
.yarn/
.cache/
.metro-health-check*
.expo/
.expo-shared/
.meteor/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Metro cache (sometimes created at project root)
.metro-cache/

# --- OS / editor ---
.DS_Store
Thumbs.db
*.swp
*.tsbuildinfo
.idea/
.vscode/*
!.vscode/settings.json

# --- Env / tooling helpers ---
.env
.env.*
!.env.example
.envrc
.direnv/

# Optional npm/yarn config that can contain tokens (only commit if you *need* them and they’re sanitized)
.npmrc
.yarnrc
.yarnrc.yml

# --- Expo / Web ---
expo-env.d.ts

# --- EAS (keep eas.json, ignore local build/update artifacts) ---
.eas/local/
.eas/build/
.eas/update/

# --- Native (Expo managed; keep ignored even if you eject) ---
ios/
android/
Pods/
*.orig.*
.kotlin/
.gradle/
.cxx/

# Android/Gradle extras that can appear outside android/ (if you ever unignore android/)
*.iml
local.properties
gradle.properties
/build/

# --- Signing / credentials (NEVER COMMIT) ---
*.jks
*.keystore
*.p8
*.p12
*.key
*.pem
*.crt
*.cer
*.csr
*.mobileprovision

# Firebase native configs (only used in bare/ejected)
google-services.json
GoogleService-Info.plist

# Firebase admin / service accounts (server-side only)
serviceAccount*.json
firebase-adminsdk-*.json

# Sentry auth (if present)
sentry.properties

# --- Firebase Emulators / Functions (local, can contain secrets) ---
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
storage-debug.log
.functions-cache/
.runtimeconfig.json

# --- Optional: large binaries from builds ---
*.apk
*.aab
*.ipa
*.app
*.xcarchive
